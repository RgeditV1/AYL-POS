[project]
name = "ayl-pos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "tkcalendar>=1.6.1",
]

[project.scripts]
ayl-pos = "src.main:run"

[tool.setuptools]
packages = ["src", "src.core", "src.gui", "src.legacy"]
